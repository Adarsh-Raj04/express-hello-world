const app = require('./app')
// hello this is code code
const port = process.env.PORT || 3000

app.listen(port, () => {
  console.log(`Example app listening at http://localhost:${port}`)
})
// const app = require('./app')
// // hello this is code code
// const port = process.env.PORT || 3000

// app.listen(port, () => {
//   console.log(`Example app listening at http://localhost:${port}`)
// })
